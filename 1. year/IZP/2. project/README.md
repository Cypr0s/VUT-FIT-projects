This projects is about analyzing bitmap files to find the longest horizontal or vertical line and the largest square of 1's. It also validates the bitmap format and outputs the coordinates of the detected patterns.

# 12.5 / 14