Program about reading contact names and phone numbers from standard input, then searching for matches based on a digit sequence (like T9 predictive text), optionally allowing interrupted sequences. It validates input and converts names to digit format.

# 5 / 10 (mostly due to the fact that that i used CamelCase)